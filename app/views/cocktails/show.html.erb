<h2><%= @cocktail.name %><small><%= link_to "🗑", cocktail_path(@cocktail), method: :delete, data: { confirm: "Delete #{@cocktail.name}?" } %></small></h2>
<div class="row">
  <div class="col-sm-8">
    <ul class="list-group">
      <% @cocktail.doses.each do |dose| %>
      <li class="list-group-item"><%= dose.description %> <%= dose.ingredient.name %>
        <%= link_to "Delete dose", dose_path(dose), method: :delete, data: { confirm: "Are you sure?" }%>
      </li>
      <% end %>
    </ul>
  </div>
  <div class="col-sm-4">
    <%= render "doses/new" %>
  </div>
</div>

<h5><%= link_to "Back to cocktails", cocktails_path, class: "btn btn-primary" %></h5>
